import 'package:flutter/material.dart';

class GrowColors {
  GrowColors._();

  static const primary = Color(0xFF40A937);
  static const primaryDark = Color(0xFF338A2B); // estado pressionado/sobreposto
  static const primaryLight = Color(0xFF6DC265);

  // Backgrounds Dark
  static const darkBg = Color(0xFF04090F);
  static const darkSurface = Color(0xFF0F1B2A); // cards / nav bar
  static const darkSurfaceElevated = Color(0xFF27323F); // cards / modals

  //Backgrounds Light
  static const lightBg = Color(0xFFF8F9FA);
  static const lightSurface = Color(0xFFFFFFFF);
  static const lightSurfaceElevated = Color(0xFFE7E8E9);

  // Text – Dark Mode
  static const darkTextPrimary = Color(0xFFE2E3DF);
  static const darkTextSecondary = Color(0xFF7D848C);
  static const darkTextDisabled = Color(0xFF555555);

  // Text – Light mode
  static const lightTextPrimary = Color(0xFF0A0A0A);
  static const lightTextSecondary = Color(0xFF555555);
  static const lightTextDisabled = Color(0xFFAAAAAA);

  // Etiquetas e status
  static const discount = Color(0xFFFF3D3D); // Exemplo: "-25%"
  static const badgeBestSeller = Color(0xFFFF9800);
  static const badgeNew = Color(0xFF40A937);
  static const success = Color(0xFF40A937);
  static const error = Color(0xFFFF3D3D);

  // Bordas/ Divisores
  static const darkBorder = Color(0xFF2A2A2A);
  static const darkBorderHighlight = Color(0xFF40A937); // ativo
  static const lightBorder = Color(0xFFDDDDDD);
  static const lightBorderHighlight = Color(0xFF338A2B);
}
