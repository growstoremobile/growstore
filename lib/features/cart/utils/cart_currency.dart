import 'package:growstore/shared/utils/price_utils.dart';

String cartCurrencyValue(double value) {
  return formatGrowCurrencyValue(value);
}

String cartCurrency(double value) {
  return formatGrowCurrency(value);
}
